<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Little Agent Chatbot | Project Presentation</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Chosen Palette: Warm Neutrals (Stone, Indigo) -->
    <!-- Application Structure Plan: A single-page application with a fixed side navigation (top bar on mobile) for easy access to key sections: Overview, Features, Architecture, and Setup. This thematic structure enhances discoverability over a linear document. An interactive HTML/CSS flowchart replaces the 'How it Works' text for better comprehension, and copy-to-clipboard functionality is added to setup commands to improve developer experience. The goal is to create an engaging project homepage from the README content. -->
    <!-- Visualization & Content Choices: Report Info: 'Features' list -> Goal: Organize/Inform -> Viz: Grid of cards with icons -> Interaction: Hover effect -> Justification: More visually engaging than a bulleted list. | Report Info: 'How it Works' text -> Goal: Explain -> Viz: Interactive HTML/CSS flowchart -> Interaction: Click to show details -> Justification: A visual diagram is superior for explaining a technical process. | Report Info: Setup commands -> Goal: Instruct -> Viz: Styled code blocks -> Interaction: Copy-to-clipboard button -> Justification: Improves usability for developers. -->
    <!-- CONFIRMATION: NO SVG graphics used. NO Mermaid JS used. -->
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8fafc; /* slate-50 */
            color: #1e293b; /* slate-800 */
        }
        .nav-link.active {
            background-color: #4f46e5; /* indigo-600 */
            color: #ffffff;
        }
        .group:hover .group-hover-effect {
            transform: translateY(-4px);
            box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
        }
        .code-block {
            position: relative;
        }
        .copy-button {
            position: absolute;
            top: 0.5rem;
            right: 0.5rem;
            background-color: #4b5563; /* gray-600 */
            color: white;
            padding: 0.25rem 0.5rem;
            border-radius: 0.375rem;
            font-size: 0.75rem;
            cursor: pointer;
            opacity: 0;
            transition: opacity 0.2s;
        }
        .code-block:hover .copy-button {
            opacity: 1;
        }
        .diagram-step {
            cursor: pointer;
            border: 2px solid transparent;
            transition: all 0.3s ease;
        }
        .diagram-step.selected {
            border-color: #4f46e5; /* indigo-600 */
            transform: scale(1.05);
        }
        .arrow {
            font-size: 2rem;
            color: #94a3b8; /* slate-400 */
        }
        @media (max-width: 768px) {
            .arrow {
                transform: rotate(90deg);
            }
        }
    </style>
</head>
<body class="flex">

    <!-- Sidebar Navigation -->
    <nav id="sidebar" class="hidden md:flex flex-col w-64 bg-white p-4 space-y-2 border-r border-slate-200 fixed h-full">
        <h1 class="text-xl font-bold text-indigo-600 mb-4">Little Agent</h1>
        <a href="#overview" class="nav-link text-slate-700 hover:bg-slate-100 p-2 rounded-lg transition-colors">Overview</a>
        <a href="#features" class="nav-link text-slate-700 hover:bg-slate-100 p-2 rounded-lg transition-colors">Features</a>
        <a href="#architecture" class="nav-link text-slate-700 hover:bg-slate-100 p-2 rounded-lg transition-colors">Architecture</a>
        <a href="#setup" class="nav-link text-slate-700 hover:bg-slate-100 p-2 rounded-lg transition-colors">Setup Guide</a>
        <a href="#license" class="nav-link text-slate-700 hover:bg-slate-100 p-2 rounded-lg transition-colors">License</a>
    </nav>

    <!-- Mobile Top Navigation -->
    <nav class="md:hidden fixed top-0 left-0 right-0 bg-white shadow-md z-10">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between items-center py-2">
                 <h1 class="text-lg font-bold text-indigo-600">Little Agent</h1>
                <select id="mobile-nav-select" class="bg-slate-100 border border-slate-300 rounded-md p-1 text-sm">
                    <option value="#overview">Overview</option>
                    <option value="#features">Features</option>
                    <option value="#architecture">Architecture</option>
                    <option value="#setup">Setup</option>
                    <option value="#license">License</option>
                </select>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="flex-1 md:ml-64 mt-16 md:mt-0 p-6 md:p-12">
        
        <!-- Overview Section -->
        <section id="overview" class="min-h-screen flex items-center">
            <div class="max-w-4xl mx-auto text-center">
                <span class="text-indigo-600 font-semibold">Local-First Conversational AI</span>
                <h2 class="text-4xl md:text-6xl font-bold mt-2 text-slate-900">Little Agent Chatbot</h2>
                <p class="mt-6 text-lg text-slate-600">
                    A simple, local-first AI agent and RAG chatbot powered by Ollama, LangChain, and Gradio. This project is a lightweight, accessible starting point for exploring local LLMs and building your own private conversational AI agents.
                </p>
                <div class="mt-8 flex justify-center gap-4">
                    <a href="#setup" class="bg-indigo-600 text-white font-semibold py-3 px-6 rounded-lg shadow-md hover:bg-indigo-700 transition-all">Get Started</a>
                    <a href="https://github.com/your-username/Little_Agent_Chatbot" target="_blank" class="bg-white text-slate-700 font-semibold py-3 px-6 rounded-lg shadow-md border border-slate-200 hover:bg-slate-50 transition-all">View on GitHub</a>
                </div>
            </div>
        </section>

        <!-- Features Section -->
        <section id="features" class="py-20">
            <div class="max-w-6xl mx-auto">
                <div class="text-center">
                    <h2 class="text-3xl font-bold text-slate-900">Core Features</h2>
                    <p class="mt-4 text-lg text-slate-600">Discover what makes Little Agent a powerful tool for local AI development.</p>
                </div>
                <div class="mt-12 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                    <!-- Feature Card -->
                    <div class="group">
                        <div class="bg-white p-6 rounded-xl shadow-sm group-hover-effect transition-all duration-300 h-full">
                            <h3 class="font-semibold text-lg">💻 Run Locally</h3>
                            <p class="mt-2 text-slate-600">Built to run entirely on your own machine. Your data and conversations stay private and secure.</p>
                        </div>
                    </div>
                     <!-- Feature Card -->
                    <div class="group">
                        <div class="bg-white p-6 rounded-xl shadow-sm group-hover-effect transition-all duration-300 h-full">
                            <h3 class="font-semibold text-lg">🤖 Powered by Ollama</h3>
                            <p class="mt-2 text-slate-600">Integrates with Ollama to run powerful open-source LLMs locally, even on consumer-grade hardware.</p>
                        </div>
                    </div>
                     <!-- Feature Card -->
                    <div class="group">
                        <div class="bg-white p-6 rounded-xl shadow-sm group-hover-effect transition-all duration-300 h-full">
                            <h3 class="font-semibold text-lg">🧠 Qwen2 Model</h3>
                            <p class="mt-2 text-slate-600">Utilizes the efficient and capable `qwen2` model, offering great performance on modest hardware.</p>
                        </div>
                    </div>
                     <!-- Feature Card -->
                    <div class="group">
                        <div class="bg-white p-6 rounded-xl shadow-sm group-hover-effect transition-all duration-300 h-full">
                            <h3 class="font-semibold text-lg">🔗 LangChain Integration</h3>
                            <p class="mt-2 text-slate-600">Leverages the LangChain framework to create intelligent agents that can do more than just chat.</p>
                        </div>
                    </div>
                     <!-- Feature Card -->
                    <div class="group">
                        <div class="bg-white p-6 rounded-xl shadow-sm group-hover-effect transition-all duration-300 h-full">
                            <h3 class="font-semibold text-lg">📄 Retrieval-Augmented Generation</h3>
                            <p class="mt-2 text-slate-600">Enhance your chatbot's knowledge by allowing it to retrieve information from your own PDF documents.</p>
                        </div>
                    </div>
                     <!-- Feature Card -->
                    <div class="group">
                        <div class="bg-white p-6 rounded-xl shadow-sm group-hover-effect transition-all duration-300 h-full">
                            <h3 class="font-semibold text-lg">🛠️ Extensible Agent Capabilities</h3>
                            <p class="mt-2 text-slate-600">Easily give the agent new tools to fetch real-time data or perform calculations.</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Architecture Section -->
        <section id="architecture" class="py-20 bg-white">
            <div class="max-w-6xl mx-auto">
                <div class="text-center">
                    <h2 class="text-3xl font-bold text-slate-900">How It Works</h2>
                    <p class="mt-4 text-lg text-slate-600 max-w-3xl mx-auto">This section provides a visual guide to the project's architecture. Click on each step in the interactive diagram below to understand the flow of information, from user input to the agent's final response.</p>
                </div>

                <div class="mt-12 flex flex-col md:flex-row items-center justify-center gap-4 md:gap-8">
                    <div id="step-1" class="diagram-step bg-slate-50 p-4 rounded-lg text-center shadow-sm w-48">
                        <div class="text-3xl">👤</div>
                        <h4 class="font-semibold mt-2">User & Gradio UI</h4>
                    </div>
                    <div class="arrow">→</div>
                    <div id="step-2" class="diagram-step bg-slate-50 p-4 rounded-lg text-center shadow-sm w-48">
                        <div class="text-3xl">🔗</div>
                        <h4 class="font-semibold mt-2">LangChain</h4>
                    </div>
                    <div class="arrow">→</div>
                    <div id="step-3" class="diagram-step bg-slate-50 p-4 rounded-lg text-center shadow-sm w-48">
                       <div class="text-3xl">🤖</div>
                        <h4 class="font-semibold mt-2">Agent Executor</h4>
                    </div>
                    <div class="arrow">→</div>
                     <div id="step-4" class="diagram-step bg-slate-50 p-4 rounded-lg text-center shadow-sm w-48">
                       <div class="text-3xl">🧠</div>
                        <h4 class="font-semibold mt-2">Ollama & LLM</h4>
                    </div>
                     <div class="arrow">→</div>
                    <div id="step-5" class="diagram-step bg-slate-50 p-4 rounded-lg text-center shadow-sm w-48">
                       <div class="text-3xl">📄</div>
                        <h4 class="font-semibold mt-2">RAG & Tools</h4>
                    </div>
                </div>

                <div id="architecture-details" class="mt-8 p-6 bg-indigo-50 rounded-lg min-h-[120px] text-center flex items-center justify-center">
                    <p class="text-indigo-800">Click on a step above to see the details.</p>
                </div>
            </div>
        </section>

        <!-- Setup Section -->
        <section id="setup" class="py-20">
            <div class="max-w-4xl mx-auto">
                <div class="text-center">
                    <h2 class="text-3xl font-bold text-slate-900">Setup Guide</h2>
                    <p class="mt-4 text-lg text-slate-600">Follow these steps to get your own local agent running in minutes.</p>
                </div>

                <div class="mt-12 space-y-8">
                    <div>
                        <h3 class="text-xl font-semibold"><span class="bg-indigo-600 text-white rounded-full w-8 h-8 inline-flex items-center justify-center mr-3">1</span>Clone the Repository</h3>
                        <div class="mt-4 bg-slate-800 text-white p-4 rounded-lg code-block">
                            <button class="copy-button" onclick="copyCode(this)">Copy</button>
                            <pre><code class="language-bash">git clone https://github.com/your-username/Little_Agent_Chatbot.git
cd Little_Agent_Chatbot</code></pre>
                        </div>
                    </div>
                    
                    <div>
                        <h3 class="text-xl font-semibold"><span class="bg-indigo-600 text-white rounded-full w-8 h-8 inline-flex items-center justify-center mr-3">2</span>Set Up Virtual Environment</h3>
                         <div class="mt-4 bg-slate-800 text-white p-4 rounded-lg code-block">
                            <button class="copy-button" onclick="copyCode(this)">Copy</button>
                            <pre><code class="language-bash">python3 -m venv .venv
source .venv/bin/activate</code></pre>
                        </div>
                        <p class="text-sm text-slate-500 mt-2">On Windows, use `.venv\Scripts\activate`</p>
                    </div>

                    <div>
                        <h3 class="text-xl font-semibold"><span class="bg-indigo-600 text-white rounded-full w-8 h-8 inline-flex items-center justify-center mr-3">3</span>Install Dependencies</h3>
                         <div class="mt-4 bg-slate-800 text-white p-4 rounded-lg code-block">
                            <button class="copy-button" onclick="copyCode(this)">Copy</button>
                            <pre><code class="language-bash">pip install langchain langchain-community langchain-core langchain-ollama chromadb sentence-transformers pypdf python-dotenv unstructured[pdf] tiktoken gradio</code></pre>
                        </div>
                    </div>

                    <div>
                        <h3 class="text-xl font-semibold"><span class="bg-indigo-600 text-white rounded-full w-8 h-8 inline-flex items-center justify-center mr-3">4</span>Download Models via Ollama</h3>
                         <div class="mt-4 bg-slate-800 text-white p-4 rounded-lg code-block">
                            <button class="copy-button" onclick="copyCode(this)">Copy</button>
                            <pre><code class="language-bash"># Pull a language model (e.g., qwen2:4b)
ollama pull qwen2:4b

# Pull the model for embeddings
ollama pull nomic-embed-text</code></pre>
                        </div>
                    </div>

                     <div>
                        <h3 class="text-xl font-semibold"><span class="bg-indigo-600 text-white rounded-full w-8 h-8 inline-flex items-center justify-center mr-3">5</span>Run the Application</h3>
                        <p class="mt-2 text-slate-600">Ensure Ollama is running, add your PDF files to a `data` folder, then launch the app:</p>
                         <div class="mt-4 bg-slate-800 text-white p-4 rounded-lg code-block">
                            <button class="copy-button" onclick="copyCode(this)">Copy</button>
                            <pre><code class="language-bash">python app.py</code></pre>
                        </div>
                        <p class="text-sm text-slate-500 mt-2">Navigate to the local URL provided by Gradio (e.g., http://127.0.0.1:7860).</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- License Section -->
        <footer id="license" class="py-12 bg-white border-t border-slate-200">
            <div class="max-w-4xl mx-auto text-center">
                 <h2 class="text-2xl font-bold text-slate-900">License</h2>
                 <p class="mt-4 text-slate-600">
                    This project is released under the **MIT License**. You are free to use, modify, and distribute it. See the <a href="https://github.com/your-username/Little_Agent_Chatbot/blob/main/LICENSE" target="_blank" class="text-indigo-600 hover:underline">LICENSE</a> file on GitHub for more details.
                 </p>
                 <p class="mt-6 text-sm text-slate-500">&copy; 2024 Little Agent Chatbot. All Rights Reserved.</p>
            </div>
        </footer>

    </main>
    
    <script>
        // Data for architecture diagram
        const architectureContent = {
            'step-1': {
                title: 'User & Gradio UI',
                description: 'The user interacts with a simple, clean web interface created by Gradio. This is the entry point for all queries and the display area for all responses.'
            },
            'step-2': {
                title: 'LangChain Orchestration',
                description: 'LangChain provides the core framework. It orchestrates the flow between the user, the retrieval system, the agent, and the LLM, managing the entire process.'
            },
            'step-3': {
                title: 'Agent Executor',
                description: 'The Agent Executor uses the LLM to reason about the user\'s query. It decides whether to answer directly, use a tool (like a calculator), or retrieve information from documents.'
            },
            'step-4': {
                title: 'Ollama & LLM (qwen2)',
                description: 'Ollama serves the open-source LLM (qwen2) locally. This is the "brain" of the operation, responsible for understanding, reasoning, and generating human-like text.'
            },
            'step-5': {
                title: 'RAG & Tools',
                description: 'For queries requiring specific knowledge, the system uses Retrieval-Augmented Generation (RAG) to search local PDF documents. It can also use other tools like a calculator for specific tasks.'
            }
        };

        // Function to copy code from code blocks
        function copyCode(button) {
            const pre = button.parentElement;
            const code = pre.querySelector('code');
            const text = code.innerText;
            navigator.clipboard.writeText(text).then(() => {
                button.innerText = 'Copied!';
                setTimeout(() => {
                    button.innerText = 'Copy';
                }, 2000);
            }).catch(err => {
                console.error('Failed to copy text: ', err);
            });
        }

        document.addEventListener('DOMContentLoaded', () => {
            // Sidebar Navigation Logic
            const sections = document.querySelectorAll('section, footer');
            const navLinks = document.querySelectorAll('#sidebar .nav-link');
            
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        navLinks.forEach(link => {
                            link.classList.remove('active');
                            if (link.getAttribute('href').substring(1) === entry.target.id) {
                                link.classList.add('active');
                            }
                        });
                    }
                });
            }, { rootMargin: '-50% 0px -50% 0px' });

            sections.forEach(section => {
                observer.observe(section);
            });
            
            // Mobile navigation
            const mobileNav = document.getElementById('mobile-nav-select');
            mobileNav.addEventListener('change', (e) => {
                window.location.hash = e.target.value;
            });


            // Architecture Diagram Interaction
            const diagramSteps = document.querySelectorAll('.diagram-step');
            const detailsContainer = document.getElementById('architecture-details');

            diagramSteps.forEach(step => {
                step.addEventListener('click', () => {
                    // Remove selected state from all steps
                    diagramSteps.forEach(s => s.classList.remove('selected'));
                    // Add selected state to the clicked step
                    step.classList.add('selected');

                    const content = architectureContent[step.id];
                    detailsContainer.innerHTML = `
                        <div class="transition-opacity duration-300">
                            <h4 class="font-bold text-lg text-indigo-900">${content.title}</h4>
                            <p class="mt-2 text-indigo-700">${content.description}</p>
                        </div>
                    `;
                });
            });
        });
    </script>
</body>
</html>

